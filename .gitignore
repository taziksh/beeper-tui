
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Superpowers brainstorming companion
.superpowers/

# Built binary (use `go install` or `go build -o` for distribution)
/beeper-tui

# Local spike/experiment binary
/spike

# Stray clone of the beads tool source (not part of this project)
/beads/
/beeper-stub

# Local secrets (TINFOIL_API_KEY etc.), sourced by make run and the launcher
.env

# macOS noise
.DS_Store
